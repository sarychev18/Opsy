#flashcards/FastAPI 
Опиши процесс загрузки веб-страницы  :: <br>1. DNS Resolution<br>Кэш браузера → кэш ОС → кэш роутера → DNS провайдера (рекурсивный запрос)<br><br>2.TCP Connection<br>TCP 3-way handshake (SYN → SYN-ACK → ACK)<br><br>3. TLS Handshake (для HTTPS)<br>Обмен сертификатами, установка шифрованного соединения<br><br>4. HTTP Request<br>Браузер отправляет HTTP-запрос (GET /page) с заголовками<br><br>5. Server Processing<br>Бэкенд (FastAPI/Django) обрабатывает запрос, возвращает HTTP-ответ:<br><br>6. Browser Rendering Pipeline <br>HTML parsing → построение DOM (Document Object Model) <br>CSS parsing → построение CSSOM (CSS Object Model)<br>JavaScript execution → модификация DOM/CSSOM<br>Render tree → Layout → Paint → Display
<!--SR:!2026-04-14,4,210-->


**Важно**
- ✅ Важно для Middle: понимать этапы 4-5 для оптимизации API и этап 6 для фронтенд-интеграции

-------------------------------------------
### Протокол HTTP
#flashcards/FastAPI 
Опиши протокол HTTP  :: **HTTP (HyperText Transfer Protocol)** — протокол прикладного уровня для передачи данных в клиент-серверной архитектуре.<br><br>1. **Методы (CRUD)**<br>`GET` — получение ресурса<br>`POST` — создание ресурса<br>`PUT` — полное обновление ресурса<br>`PATCH` — частичное обновление ресурса<br>`DELETE` — удаление ресурса<br><br>2. **Коды ответа:**<br>- **1xx** Информационные (100 Continue)<br>- **2xx** Успех (200 OK, 201 Created)<br>- **3xx** Перенаправления (301 Moved Permanently)<br>- **4xx** Ошибка клиента (404 Not Found, 400 Bad Request)<br>- **5xx** Ошибка сервера (500 Internal Server Error)
<!--SR:!2026-05-22,46,250-->

#### Cтруктура запроса
```text
GET /api/users?page=2 HTTP/1.1
Host: example.com
Content-Type: application/json
```
#### Структура ответа
```text
HTTP/1.1 200 OK
Content-Type: application/json

{"users": []}
```

**Важно**
- ✅ - Заголовки (headers) содержат мета-информацию (авторизация, тип данных)
- ✅- в HTTP-запросе указывается **домен/путь**, а IP определяется через DNS
#### Пример в FastAPI
```python
from fastapi import FastAPI

app = FastAPI()

@app.get("/users")  # GET-запрос с query-параметром
def get_users(page: int = 1):
    return {"page": page}

@app.post("/users")  # POST-запрос с телом
def create_user(user: dict):
    return {"created": user}
```

------------------------------------
### Требования к архитектуре REST

Существует 5 обязательных ограничений для построения REST-приложений и 1 необязательное.

### 1. Клиент-серверная модель

Клиент и сервер чётко разделены. Клиент отвечает за интерфейс пользователя, сервер — за хранение и обработку данных.

### 2. Отсутствие состояния (stateless)

Каждый запрос от клиента к серверу должен быть самодостаточным и не зависеть от предыдущих запросов. Вся необходимая информация передаётся в запросе.

### 3. Кэширование

Ответы сервера должны содержать информацию о том, можно ли их кэшировать. Это помогает сократить задержки и снизить нагрузку.

### 4. Единообразие интерфейса

Все взаимодействия между клиентом и сервером должны быть консистентными и предсказуемыми:

- Четкая структура URL
- Использование стандартных HTTP методов (GET, POST, PUT, DELETE и др.)
- Передача данных в стандартных форматах, чаще всего JSON или XML

### 5. Слои (layered system)

Архитектура может состоять из нескольких слоёв (клиент, сервер, прокси, балансировщики и т. д.), где каждый слой не знает о внутреннем устройстве других.

### 6. Код по запросу (опционально)

Сервер может передавать клиенту исполняемый код (например, скрипты), чтобы расширить функциональность клиента.

#flashcards/FastAPI 
Опиши требования к архитектуре REST  :: Существует 5 обязательных ограничений для построения REST-приложений и 1 необязательное:<br><br>Требования к REST-архитектуре (6 ограничений по Филдингу):<br>1. Единообразие интерфейса (Uniform Interface)<br>- Единый способ взаимодействия (HTTP-методы, URI, статус-коды)<br>- Ресурсы идентифицируются через URI (`/api/users/1`)<br>- Самодостаточные сообщения (заголовки + тело)<br><br>2. Клиент-серверное разделение (Client-Server)<br>- Каждый запрос от клиента к серверу должен быть самодостаточным и не зависеть от предыдущих запросов. Вся необходимая информация передаётся в запросе.<br><br>3. Отсутствие состояния (Stateless)<br>- Каждый запрос содержит всю информацию для его обработки<br>- Сервер не хранит состояние клиента между запросами<br><br>4. Кэшируемость (Cacheability)<br>- Ответы должны явно указывать, можно ли их кэшировать<br>- Снижает нагрузку, повышает производительность<br><br>5. Многоуровневая система (Layered System)<br>- Архитектура может состоять из нескольких слоёв (клиент, сервер, прокси, балансировщики и т. д.), где каждый слой не знает о внутреннем устройстве других.<br><br>6. Код по требованию (Code on Demand, optional)<br>- Сервер может расширять функциональность клиента, передавая исполняемый код (например, JavaScript)
<!--SR:!2026-04-11,1,208-->